name: Notify Slack 2

on:
  pull_request:
    types: [opened, edited, reopened]  # Puedes ajustar los tipos de eventos según tus necesidades

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Send Slack notification
        uses: slackapi/slack-github-action@v1.24.0
        with:
          channel-id: 'C07G7NCBVQW'
          payload: |
            {
              
              "text": "📢 Atención: Se ha creado un nuevo Pull Request."
            }
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
